#include <iostream>

using namespace std;

// // 전부 -1로 초기화
// int cache[2500][2500];
// int someObscureFunction(int a, int b) {
//   if(...) return ...;
//   int& ret = cache[a][b];
//   if(ret != -1) return ret;
//   // 답 계산
//   ...
//   return ret;
// }

int main() {
  // 배열 초기화
  // memset(cache, -1, sizeof(cache));
}